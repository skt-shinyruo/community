# Context

项目领域词汇表。只收录术语定义，不记录决策与实现细节。

## 前台交互与信息架构（UI IA）

- **一级域（top-level domain）**：侧边栏承载的顶级导航目的地——社区（帖子/搜索/收藏/我的主页）、市场、个人（积分钱包/网盘/通知/私信/设置）。域内二级页面不进侧边栏。
- **域内 Tab（in-domain tabs）**：一级域页面内部的二级目的地切换，如市场域的我的出售/我的购买/出售订单。
- **壳搜索（shell search）**：Topbar 中的全局搜索框，搜索社区统一索引（帖子/标签/用户）并跳转 `/search`，⌘K/Ctrl+K 聚焦。区别于各域页内工具栏搜索。
- **账户区（account zone）**：侧边栏底部的用户身份卡（头像/名字/角色 → 我的主页，尾随设置/登出）。
- **未读角标（unread badge）**：导航项上的未读数标记，用于侧边栏「通知」「私信」项。
- **反馈渠道（feedback channel）**：操作反馈的三种通道——静默（结果在屏上立即可见）、toast（结果不可见或伴随跳转的全局提示）、内联（定位到字段/区块的就地提示）。
- **骨架屏（skeleton）**：列表/详情首载时的结构占位形态，由 UiSkeleton 原语承载；加载态不由 UiState 承担。
- **确认弹窗（confirm modal）**：不可逆/资损风险/影响他人的操作在执行前的确认对话（UiModalConfirm）；可逆/单人操作与登出免确认。
- **消费流（consumption stream）**：以浏览消费为主的列表（帖子/市场/通知/会话/收藏/评论），分页采用"加载更多"追加式；UiPagination 仅用于管理/表格域。
- **基础层（foundation）**：全部前台页面共同依赖的视觉语言、交互原语与产品壳层；它不是用户可访问的业务区域。
- **页面簇（page cluster）**：UI 迁移的实施分组，聚合共享用户目标和交互模式的一组页面；它不同于一级域，也不同于后端 owner domain。
- **scoped SFC 约定**：页面样式的唯一组织方式——`<style scoped>`（超长可拆同目录外部 scoped CSS）；全局样式表只保留令牌/base/壳布局/工具四类。
- **原语内部类（primitive-internal class）**：`.btn/.input/.card` 等全局类是 Ui* 原语的实现细节，视图禁止直接使用；链接外观按钮走 UiButton 的 `to`/`href` 形态。

## 前台 UI 迁移

- **迁移波次（migration wave）**：一个可独立验收的前台 UI 迁移阶段；可以由多个小 PR 完成，但整波通过验收门后才能进入下一波。
- **页面合同（page contract）**：最终 UI spec 中对应一个正式前台路由的约束条目，记录共享规范下的页面差异和验收责任；它不是逐页像素稿。
