analytics:
  storage: memory
  max-days-range: 31

security:
  jwt:
    hmac-secret: test-jwt-secret-please-change-at-least-32bytes

community:
  metrics:
    basic-auth:
      username: prometheus
      password: test-prometheus-pass-please-change

spring:
  application:
    name: analytics-service
