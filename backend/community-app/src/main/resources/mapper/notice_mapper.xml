<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.notice.mapper.NoticeMapper">

    <!--
      Rollout note: legacy notice rows with from_id = 1 are still treated as notices
      only for the known notice topics below. Real user 1 conversations keep numeric
      conversation ids like "1_2" and must stay visible as letters.
    -->
    <sql id="noticePredicate">
        (
            from_id = 0
            or (
                from_id = 1
                and conversation_id in ('comment', 'like', 'follow', 'moderation')
            )
        )
    </sql>

    <insert id="insertNotice" parameterType="com.nowcoder.community.notice.entity.NoticeRecord" keyProperty="id">
        insert into message(from_id, to_id, conversation_id, content, status, create_time)
        values(#{senderUserId}, #{recipientUserId}, #{topic}, #{content}, #{status}, #{createTime})
    </insert>

    <select id="selectNotices" resultType="com.nowcoder.community.notice.entity.NoticeRecord">
        select id,
               from_id as senderUserId,
               to_id as recipientUserId,
               conversation_id as topic,
               content,
               status,
               create_time as createTime
        from message
        where status != 2
          and <include refid="noticePredicate"/>
          and to_id = #{userId}
          and conversation_id = #{topic}
        order by create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="selectNoticeCount" resultType="int">
        select count(id) from message
        where status != 2
          and <include refid="noticePredicate"/>
          and to_id = #{userId}
          and conversation_id = #{topic}
    </select>

    <select id="selectNoticeUnreadCount" resultType="int">
        select count(id) from message
        where status = 0
          and <include refid="noticePredicate"/>
          and to_id = #{userId}
          <if test="topic!=null">
              and conversation_id = #{topic}
          </if>
    </select>

    <update id="updateNoticesStatusForRecipient">
        update message set status = #{status}
        where <include refid="noticePredicate"/>
          and to_id = #{userId}
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
