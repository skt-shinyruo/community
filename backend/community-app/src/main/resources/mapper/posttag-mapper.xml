<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.content.infrastructure.persistence.mapper.PostTagMapper">

    <insert id="insertPostTag">
        insert into post_tag(post_id, tag_id, create_time)
        values(#{postId, jdbcType=BINARY}, #{tagId, jdbcType=BINARY}, #{createTime})
    </insert>

    <select id="selectTagNamesByPostIds" resultType="com.nowcoder.community.content.domain.model.PostTagName">
        select
            pt.post_id as post_id,
            t.name as name
        from post_tag pt
        join tag t on pt.tag_id = t.id
        where pt.post_id in
        <foreach collection="postIds" item="pid" open="(" separator="," close=")">
                #{pid, jdbcType=BINARY}
            </foreach>
        order by pt.post_id asc, t.name asc
    </select>

    <delete id="deleteTagsByPostId">
        delete from post_tag where post_id = #{postId, jdbcType=BINARY}
    </delete>

</mapper>
