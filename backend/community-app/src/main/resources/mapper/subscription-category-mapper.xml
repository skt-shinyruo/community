<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.content.mapper.SubscriptionCategoryMapper">

    <insert id="insertSubscription">
        insert ignore into user_subscription_category(user_id, category_id, create_time)
        values(#{userId}, #{categoryId}, #{createTime})
    </insert>

    <delete id="deleteSubscription">
        delete from user_subscription_category
        where user_id = #{userId}
          and category_id = #{categoryId}
    </delete>

    <select id="existsSubscription" resultType="int">
        select count(*)
        from user_subscription_category
        where user_id = #{userId}
          and category_id = #{categoryId}
    </select>

    <select id="selectSubscribedCategoryIds" resultType="int">
        select category_id
        from user_subscription_category
        where user_id = #{userId}
        order by create_time desc, category_id desc
    </select>

</mapper>

