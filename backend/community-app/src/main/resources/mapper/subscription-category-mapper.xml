<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.content.infrastructure.persistence.mapper.SubscriptionCategoryMapper">

    <select id="selectSubscribedCategoryIds" resultType="java.util.UUID">
        select category_id
        from user_subscription_category
        where user_id = #{userId, jdbcType=BINARY}
        order by create_time desc, category_id desc
    </select>

</mapper>
