<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nowcoder.community.wallet.mapper.TransferOrderMapper">

    <select id="selectByRequestId" resultType="com.nowcoder.community.wallet.entity.TransferOrder">
        select order_id, request_id, from_user_id, to_user_id, amount, status, remark, create_time, update_time
        from transfer_order
        where request_id = #{requestId}
    </select>

    <insert id="insert" parameterType="com.nowcoder.community.wallet.entity.TransferOrder">
        insert into transfer_order(order_id, request_id, from_user_id, to_user_id, amount, status, remark)
        values(#{orderId, jdbcType=BINARY}, #{requestId}, #{fromUserId, jdbcType=BINARY}, #{toUserId, jdbcType=BINARY}, #{amount}, #{status}, #{remark})
    </insert>

</mapper>
