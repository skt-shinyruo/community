<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nowcoder.community.wallet.mapper.WithdrawOrderMapper">

    <select id="selectByRequestId" resultType="com.nowcoder.community.wallet.entity.WithdrawOrder">
        select order_id, request_id, user_id, amount, status, payee_account, failure_reason, create_time, update_time
        from withdraw_order
        where request_id = #{requestId}
    </select>

    <insert id="insert" parameterType="com.nowcoder.community.wallet.entity.WithdrawOrder" useGeneratedKeys="true" keyProperty="orderId" keyColumn="order_id">
        insert into withdraw_order(request_id, user_id, amount, status, payee_account, failure_reason)
        values(#{requestId}, #{userId}, #{amount}, #{status}, #{payeeAccount}, #{failureReason})
    </insert>

</mapper>
