spring:
  application:
    name: community-app-test
  datasource:
    url: jdbc:h2:mem:community;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;NON_KEYWORDS=USER
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
  kafka:
    bootstrap-servers: 127.0.0.1:9092
    listener:
      auto-startup: false

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-handlers-package: com.nowcoder.community.infra.persistence.mybatis
  configuration:
    map-underscore-to-camel-case: true

security:
  jwt:
    hmac-secret: test-jwt-secret-please-change-at-least-32bytes
    issuer: community-auth

community:
  metrics:
    basic-auth:
      username: prometheus
      password: test-prometheus-pass-please-change

auth:
  refresh:
    store: memory
  captcha:
    store: memory
    ttl-seconds: 60
    max-failures: 3
  password-reset:
    reset-base-url: http://localhost:8080
    store: memory
    ttl-seconds: 600
    expose-reset-link: true
  registration:
    pending-user:
      ttl-seconds: 1800
      cleanup-interval-ms: 300000
    code:
      store: memory
      ttl-seconds: 600
      max-failures: 3
      resend-cooldown-seconds: 60
      expose-code: true
    mail:
      enabled: false
  login-rate-limit:
    captcha-required-failures-per-ip: 5
    captcha-required-failures-per-user: 2

content:
  storage: memory
  events:
    publisher: local
  score:
    refresh:
      enabled: false

events:
  outbox:
    enabled: false

user:
  events:
    publisher: memory

social:
  storage: memory
  events:
    publisher: local

search:
  storage: memory
  idempotency:
    cleanup-enabled: false
    retention-days: 1

analytics:
  storage: memory
  max-days-range: 31

message:
  idempotency:
    cleanup-enabled: false
    retention-days: 1

clients:
  user:
    fail-open: true
  social:
    fail-open: true

http:
  idempotency:
    enabled: true
    store: DB

xxl:
  job:
    enabled: false
