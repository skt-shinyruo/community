spring:
  application:
    name: im-core
  cloud:
    nacos:
      discovery:
        server-addr: ${SPRING_CLOUD_NACOS_DISCOVERY_SERVER_ADDR:${NACOS_SERVER_ADDR:localhost:8848}}
  datasource:
    # Local HA keeps MySQL single-primary for writes; switch IM_CORE_DB_PRIMARY_HOST after manual promotion.
    url: ${IM_CORE_DB_URL:jdbc:mysql://${IM_CORE_DB_PRIMARY_HOST:localhost}:3306/im_core?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${IM_CORE_DB_USER:im_core}
    password: ${IM_CORE_DB_PASSWORD:imcorepass}
  kafka:
    # Comma-separated broker list, for example kafka-1:9092,kafka-2:9092,kafka-3:9092.
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:29092}
    consumer:
      group-id: ${IM_CORE_CONSUMER_GROUP:im-core}
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.nowcoder.community.im.common.*
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: true

server:
  port: 18082

community:
  logging:
    service-version: ${SERVICE_VERSION:@project.version@}

security:
  jwt:
    hmac-secret: ${JWT_HMAC_SECRET:}
    issuer: ${JWT_ISSUER:community-auth}

im:
  kafka:
    command:
      concurrency: ${IM_CORE_KAFKA_COMMAND_CONCURRENCY:3}
  room:
    max-members: ${IM_ROOM_MAX_MEMBERS:10000}
  room-member-change:
    # kafka publishes RoomMemberChanged to ImTopics.EVENT_ROOM_MEMBER_CHANGED.
    publisher: ${IM_ROOM_MEMBER_CHANGE_PUBLISHER:kafka}
  cors:
    allowed-origins: ${IM_CORS_ALLOWED_ORIGINS:}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
