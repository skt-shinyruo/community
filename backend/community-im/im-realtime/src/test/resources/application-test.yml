spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
  kafka:
    bootstrap-servers: localhost:0
    listener:
      auto-startup: false

security:
  jwt:
    hmac-secret: im-realtime-test-jwt-hmac-secret-please-change-123456
    issuer: community-auth
